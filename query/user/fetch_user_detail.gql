query ($id: uuid) {
  users(where: { id: { _eq: $id } }) {
    first_name
    last_name
    email
    phone_number
    middle_name
    username
    profile_images {
      id
      image_url
      __typename
    }
    __typename
  }
}
